canal:
  server: ${CANAL_SERVER_IP:}:${CANAL_SERVER_PORT}
  destination: example
spring:
  application:
    name: canal-example
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=UTF8&useSSL=false
    username: root
    password: root1234
  jooq:
    sql-dialect: mysql
  flyway:
    baseline-on-migrate: true